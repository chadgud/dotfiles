# -- Changing Window Focus --

# change window focus within space
alt - j : yabai -m window --focus south
alt - k : yabai -m window --focus north
alt - h : yabai -m window --focus west
alt - l : yabai -m window --focus east

#change focus between external displays (left and right)
alt - s: yabai -m display --focus west
alt - g: yabai -m display --focus east

# -- Modifying the Layout --

# rotate layout clockwise
ctrl + alt - r : yabai -m space --rotate 270

# flip along y-axis
ctrl + alt - y : yabai -m space --mirror y-axis

# flip along x-axis
ctrl + alt - x : yabai -m space --mirror x-axis

# toggle window float
ctrl + alt - t : yabai -m window --toggle float --grid 4:4:1:1:2:2


# -- Modifying Window Size --

# maximize a window
ctrl + alt - m : yabai -m window --toggle zoom-fullscreen

# balance out tree of windows (resize to occupy same area)
ctrl + alt - b : yabai -m space --balance

# -- Moving Windows Around --

# swap windows
ctrl + alt - j : yabai -m window --swap south
ctrl + alt - k : yabai -m window --swap north
ctrl + alt - h : yabai -m window --swap west
ctrl + alt - l : yabai -m window --swap east

# move window and split
shift + alt - j : yabai -m window --warp south
shift + alt - k : yabai -m window --warp north
shift + alt - h : yabai -m window --warp west
shift + alt - l : yabai -m window --warp east

# move window to display left and right
ctrl + alt - s : yabai -m window --display west; yabai -m display --focus west;
ctrl + alt - g : yabai -m window --display east; yabai -m display --focus east;


# move window to prev and next space
ctrl + alt - p : yabai -m window --space prev;
ctrl + alt - n : yabai -m window --space next;

# move window to space #
ctrl + alt - 1 : yabai -m window --space 1;
ctrl + alt - 2 : yabai -m window --space 2;
ctrl + alt - 3 : yabai -m window --space 3;
ctrl + alt - 4 : yabai -m window --space 4;
ctrl + alt - 5 : yabai -m window --space 5;
ctrl + alt - 6 : yabai -m window --space 6;
ctrl + alt - 7 : yabai -m window --space 7;
ctrl + alt - 8 : yabai -m window --space 8;
ctrl + alt - 9 : yabai -m window --space 9;
# ctrl + alt - 1 : yabai -m window --space 1; osascript -e 'tell application "System Events" to key code 18 using control down'
# ctrl + alt - 2 : yabai -m window --space 2; osascript -e 'tell application "System Events" to key code 19 using control down'
# ctrl + alt - 3 : yabai -m window --space 3; osascript -e 'tell application "System Events" to key code 20 using control down'
# ctrl + alt - 4 : yabai -m window --space 4; osascript -e 'tell application "System Events" to key code 21 using control down'
# ctrl + alt - 5 : yabai -m window --space 5; osascript -e 'tell application "System Events" to key code 23 using control down'
# ctrl + alt - 6 : yabai -m window --space 6; osascript -e 'tell application "System Events" to key code 22 using control down'
# ctrl + alt - 7 : yabai -m window --space 7; osascript -e 'tell application "System Events" to key code 26 using control down'
# ctrl + alt - 8 : yabai -m window --space 8; osascript -e 'tell application "System Events" to key code 28 using control down'
# ctrl + alt - 9 : yabai -m window --space 9; osascript -e 'tell application "System Events" to key code 25 using control down'

# stop, start, restart yabi
ctrl + alt - q : yabai --stop-service;
ctrl + alt - w : yabai --start-service;
ctrl + alt - e : yabai --stop-service; yabai --start-service
